network:
  version: 2
  renderer: networkd
  ethernets:
    {{ primary_interface }}:
      addresses:
        - {{ static_ip }}
      gateway4: {{ gateway_ip }}
      nameservers:
        addresses:
          - {{ dns_servers.split()[0] }}
          - {{ dns_servers.split()[1] }}


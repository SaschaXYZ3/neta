# netplan_static_template.j2
network:
  version: 2
  ethernets:
    {{ interface_name }}:
      addresses:
        - {{ static_ip }}
      gateway4: {{ gateway_ip }}
      nameservers:
        addresses:
          {% for dns in dns_servers.split() %}
          - {{ dns }}
          {% endfor %}


$TTL    604800
@       IN      SOA     ns1.{{ domain }}. root.{{ domain }}. (
                              2         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL

; Name servers
@       IN      NS      ns1.{{ domain }}.

; PTR records
{{ dns_record_ip.split('.')[3] }}      IN      PTR     {{ dns_record_fqdn }}.

